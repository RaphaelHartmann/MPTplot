# MPTplot

*R* package for writing LaTeX code for drawing multinomial processing tree (MPT) models with the tikzpicture package of LaTeX.

# Installation
```
remotes::install_github("RaphaelHartmann/MPTplot")
```
